fun main()
{
    val s= readlnOrNull()?.split(" ")?.let {
        s->s.forEach{
        it.forEachIndexed { i,el->if (el !in it) println("no ${el}") else println("yes ${el}") }
    }
    }///it===s.map{}
    }